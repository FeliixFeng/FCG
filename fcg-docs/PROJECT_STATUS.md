# FCG 项目状态（PROJECT_STATUS）

> 更新时间：2026-04-21（夜间）

## 项目概况

- 项目：Family Care Guardian（家庭健康管理系统）
- 架构：Spring Boot 3 + Vue 3 + MySQL
- 目标：围绕家庭成员“用药计划 -> 今日任务 -> 打卡记录 -> 健康数据”形成闭环

## 当前已完成（以现代码为准）

### 核心业务

- 药品管理：药箱 CRUD、OCR 识别、计划创建
- 首页任务中心：
  - 下一条待处理主任务卡
  - 待处理任务列表
  - 今日已处理列表
  - 剂量与单位展示（如“每次 2 片”）
- 计划联表逻辑：
  - 首页按计划生成当天任务
  - 叠加记录状态（无记录时也可直接打卡）
- 打卡与库存：
  - 首次打卡/跳过可直接创建记录
  - 状态变为“已服”时自动扣库存（按计划剂量，最少 1）
- 健康模块：体征录入、趋势图、周报

### 最近一轮关键更新（2026-04-21）

- 首页（Home）
  - 打卡主卡支持“上一条/下一条”浏览任务
  - 主按钮状态随任务变化（打卡 / 已打卡 / 已跳过）
  - 移动端布局压缩与对齐优化（避免操作区留白过大）
  - 快捷入口支持直达具体功能（新建计划、低库存、录入体征、查看周报）
- 药品页（Medicine）
  - 今日待服统计口径调整为“今日未打卡任务数（recordStatus=0）”
  - OCR 录入支持移动端拍照入口（拍照 / 相册）
  - 药品删除恢复真实调用（不再是假成功）
- 健康页（Health）
  - 体征录入回车保存补齐（血压/血糖/体重统一）
  - 周报生成失败前端弹窗细分（认证失败、额度限制、超时、网络异常）
- AI 助手页（AI）
  - 流式对话已落地（SSE），前端逐步渲染与自动跟随滚动
  - 后端新增聚合上下文接口：`GET /api/ai/context`
  - 对话上下文改为聚合接口单点获取（成员档案 + 今日任务 + 今日体征 + 近7天趋势 + 最新周报）
  - 上下文短缓存（前端 30s / 后端 20s）与页面预取，减少等待体感
  - 对话区改为“中间独立滚动”，并完成桌面/移动端空间压缩优化
- 后端链路
  - 服药记录接口增加最小权限兜底（防止跨成员误操作）
  - 周报生成改为基于结构化体征摘要（最新值/均值/异常次数）
  - 风险等级从“仅依从率”升级为“依从率 + 体征异常”综合判断

### 权限与成员

- 家庭级账号 + 成员切换
- 管理员可查看/切换其他家庭成员数据
- 受控成员支持关怀模式

### 数据一致性

- `med_record` 已有唯一索引：
  - `(user_id, plan_id, scheduled_date, slot_name)`
- 避免同用户同计划同日期同时间段重复记录

## 当前业务链路（关键）

1. 药品页创建 `med_plan`
2. 首页调用 `/api/medicine/plan/records?scheduledDate=...`
3. 后端按 `med_plan` 生成今日任务，叠加 `med_record` 状态
4. 前端打卡：
   - 有 `recordId`：更新记录
   - 无 `recordId`：创建记录
5. 若状态更新为“已服”，后端执行库存扣减

## 接下来建议（按优先级）

### P0（保证可演示稳定）

1. 用药计划接口补“角色权限后端收口”
  - 普通成员仅允许操作自己的计划
  - 管理员允许为家庭成员操作
2. 药品删除前增加“仍被计划引用”提示（前后端协同）
3. 周报生成增加 AI 失败兜底文案（后端 fallback 模板）

### P1（体验优化）

4. 健康周报正文加入“引用指标高亮”（最新值、均值、异常次数）
5. 药品页 OCR 区增加“拍照后继续拍/立即识别”引导
6. 首页与药品页统计文案统一（待打卡、已打卡、超时定义）

### P2（后续完善）

7. 关怀页面（受控成员）专项优化：大字高对比、流程简化、误触防护
8. 管理界面补齐（成员管理深化 + 药品管理 + 系统设置 + 数据统计）
9. 前端包体优化（chunk 拆分）与自动化回归用例

## 开发分支建议

- `dev`：日常联调与功能开发
- `main`：稳定发布线
