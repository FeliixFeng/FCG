# FCG 首页优化功能验证报告

**日期**: 2026-03-18  
**提交**: commit `1f29074` on branch `dev`  
**状态**: ✅ 所有功能已实现并推送到远程

---

## 功能清单

### 1. ✅ 登录/注册回车键快捷提交

**文件**: `src/components/auth/AuthDialog.vue`

**实现验证**:
- ✅ 登录表单：家庭账号输入框 `@keyup.enter="handleLogin"` (L178)
- ✅ 登录表单：密码输入框 `@keyup.enter="handleLogin"` (L183)
- ✅ 注册表单：家庭账号输入框 `@keyup.enter="handleRegister"` (L205)
- ✅ 注册表单：管理员昵称输入框 `@keyup.enter="handleRegister"` (L210)
- ✅ 注册表单：密码输入框 `@keyup.enter="handleRegister"` (L213)
- ✅ 注册表单：确认密码输入框 `@keyup.enter="handleRegister"` (L218)
- ✅ 注册表单：家庭名称输入框 `@keyup.enter="handleRegister"` (L224)

**预期行为**:
- 在任意输入框按下回车键，触发对应的登录或注册操作
- 无需点击按钮，提升操作效率

**测试建议**:
```
1. 打开登录弹窗
2. 输入账号密码
3. 在任意输入框按回车键
4. 验证登录是否成功触发

5. 切换到注册标签
6. 填写注册信息
7. 在任意输入框按回车键
8. 验证注册是否成功触发
```

---

### 2. ✅ 关怀模式保留"切换成员"功能

**文件**: `src/components/common/BaseLayout.vue`

**实现验证**:
- ✅ "切换成员" dropdown-item 已移除 `v-if="!isCareMode"` 条件 (L130-138)
- ✅ 在关怀模式下，"切换成员"选项始终可见

**预期行为**:
- 受控成员（role=2）进入关怀模式后，可以切换到其他成员
- 防止老人被锁定在某个成员视图无法切换

**测试建议**:
```
1. 登录 zhangfamily / 123456
2. 选择张爷爷（role=2，关怀模式自动激活）
3. 点击右上角用户头像下拉菜单
4. 验证"切换成员"选项是否可见
5. 点击后能否正常跳转到成员选择页
```

---

### 3. ✅ 手动刷新按钮与自动刷新

**文件**: `src/views/Home.vue`

**实现验证**:
- ✅ `refreshData()` 函数实现 (L89-92)
- ✅ 刷新按钮 UI (L249-261)
- ✅ 刷新按钮旋转动画 `.refresh-loading` + `@keyframes spin` (L650-657)
- ✅ `checkin()` 函数打卡后调用 `loadTodayData()` (L136)
- ✅ `skipRecord()` 函数跳过后调用 `loadTodayData()` (L146)

**CSS 样式**:
```css
.refresh-btn (L631-648)
.refresh-loading (L650-652)
@keyframes spin (L654-657)
```

**预期行为**:
- 点击刷新按钮，图标旋转，数据重新加载
- 显示 Toast "刷新成功"
- 打卡或跳过后，数据自动刷新，无需手动点击

**测试建议**:
```
1. 进入首页
2. 点击主任务卡右上角刷新按钮
3. 验证：图标是否旋转、Toast 提示、数据是否更新

4. 打卡一个用药计划
5. 验证：数据是否自动刷新，主任务卡是否更新为下一条待办

6. 切换到其他成员打卡
7. 返回当前成员，点击刷新按钮
8. 验证：能否看到其他成员的打卡情况
```

---

### 4. ✅ 超时未服药品高亮预警

**文件**: `src/views/Home.vue`

**实现验证**:
- ✅ `getOverdueMinutes(scheduledTime)` 函数 (L101-109)
  - 返回超时分钟数，0 表示未超时
  - 实时计算，无需定时器
- ✅ `nextPendingOverdueMin` computed (L124-127)
  - 计算主任务卡超时分钟数
- ✅ 主任务卡超时标签 (L279-281)
  - 显示 "已超时 X 分钟"
- ✅ 用药列表超时高亮 (L332, L844-842)
  - 添加 `.med-overdue` class
- ✅ 用药列表超时文字 (L344-348)
  - 显示 "· 超时 X 分钟"

**CSS 样式**:
```css
.task-overdue-tag (L719-728)       - 主任务卡超时标签（红色背景）
.med-item.med-overdue (L839-842)   - 超时药品边框（橙红色）
.med-overdue .med-dot (L860)       - 超时药品圆点（橙红色）
.overdue-label (L889-892)          - 超时文字（红色加粗）
```

**预期行为**:
- 当前时间超过计划服药时间时，自动高亮显示
- 主任务卡显示红色超时标签
- 用药列表中超时项：橙红色边框、橙红色圆点、红色文字

**测试建议**:
```
方案 A：创建测试数据
1. 在药品管理页创建一个用药计划
2. 设置计划时间为过去时间（例如：08:00，当前时间 10:00）
3. 返回首页，验证超时显示

方案 B：修改系统时间（需服务器访问）
1. 创建未来时间的用药计划（例如：18:00）
2. 修改服务器系统时间到 19:00
3. 刷新首页，验证超时显示

预期效果：
- 主任务卡显示 "已超时 120 分钟"（假设超时 2 小时）
- 用药列表中该药品：
  - 边框变为橙红色
  - 圆点变为橙红色
  - 元数据显示 "· 超时 120 分钟"（红色文字）
```

---

### 5. ✅ 空状态引导优化

**文件**: `src/views/Home.vue`

**实现验证**:
- ✅ 空状态卡片 `.med-empty-state` (L314-323)
- ✅ 大图标（48x48 心形药品图标）
- ✅ 提示文字 "今日暂无用药计划"
- ✅ 温馨提示 "休息一天，享受健康时光 😊"
- ✅ "添加用药计划" 按钮，点击跳转到 `{ name: 'medicine' }`

**CSS 样式**:
```css
.med-empty-state (L775-782)        - 空状态容器（居中、padding）
.med-empty-state svg (L784-787)    - 图标样式（透明度）
.empty-text (L789-794)             - 主标题（灰色、加粗）
.empty-hint (L796-800)             - 副标题（浅灰色）
.empty-btn (L802-808)              - 按钮样式（主色调）
```

**预期行为**:
- 当 `planRecords.length === 0` 时显示空状态卡片
- 替换原来的简单文字提示，提供明确的行动指引
- 点击按钮跳转到药品管理页

**测试建议**:
```
方案 A：新用户测试
1. 注册一个新家庭
2. 选择管理员成员进入首页
3. 验证空状态卡片显示

方案 B：删除现有计划
1. 进入药品管理页
2. 删除所有用药计划
3. 返回首页
4. 验证空状态卡片显示

预期效果：
- 显示大图标（心形药品图标，灰色）
- 显示 "今日暂无用药计划"
- 显示 "休息一天，享受健康时光 😊"
- 显示绿色按钮 "添加用药计划"
- 点击按钮跳转到药品页
```

---

### 6. ✅ 概览卡体征点击跳转

**文件**: `src/views/Home.vue`

**实现验证**:
- ✅ 体征录入区域添加 `@click` 事件 (L426-429)
- ✅ 点击跳转到 `{ name: 'health' }`
- ✅ 添加 `title` 提示 "点击跳转到健康页"
- ✅ 添加 `.overview-clickable` class

**CSS 样式**:
```css
.overview-clickable (L1039-1042)       - 鼠标指针、过渡动画
.overview-clickable:hover (L1044-1048) - hover 效果（上移、阴影、背景加深）
```

**预期行为**:
- 鼠标悬停在体征录入区域，显示指针手型
- hover 时卡片上移 2px，背景加深，阴影加强
- 点击后跳转到健康数据录入页

**测试建议**:
```
1. 进入首页
2. 将鼠标悬停在 "体征已录" 概览项上
3. 验证：鼠标是否变为手型、卡片是否上移、背景是否变深
4. 点击该区域
5. 验证：是否跳转到健康页（路由名称：health）
```

---

### 7. ✅ 关怀模式时间线优化

**文件**: `src/views/Home.vue`

**实现验证**:
- ✅ `timeline` computed 关怀模式过滤逻辑 (L153-186)
  - 关怀模式下：`filter(r => r.recordStatus !== 2)` (L162)
  - 只显示待服（status=0）和已服（status=1）
  - 隐藏已跳过（status=2）

**预期行为**:
- 关怀模式下，时间线不显示 "已跳过" 项
- 减少老人的信息干扰，聚焦待完成任务

**测试建议**:
```
1. 登录 zhangfamily / 123456
2. 选择张爷爷（role=2，关怀模式自动激活）
3. 跳过一个用药计划（点击"跳过"按钮）
4. 观察时间线
5. 验证：时间线是否不显示已跳过的项

6. 切换到张爸爸（role=0，标准模式）
7. 观察时间线
8. 验证：时间线是否显示所有项（包括已跳过）
```

---

## 代码统计

### 修改文件
- `src/components/auth/AuthDialog.vue` - 7 处 `@keyup.enter` 绑定
- `src/components/common/BaseLayout.vue` - 移除 1 处 `v-if` 条件
- `src/views/Home.vue` - 主要修改

### Home.vue 详细统计
| 功能模块 | 新增/修改内容 | 行数 |
|---------|--------------|------|
| **数据刷新** | `refreshData()` + 刷新按钮 | ~30 行 |
| **超时检测** | `getOverdueMinutes()` + computed | ~20 行 |
| **空状态** | `.med-empty-state` 卡片 | ~15 行 |
| **交互优化** | 点击跳转 + 关怀时间线 | ~30 行 |
| **CSS 样式** | 8 个新样式类 + 1 个动画 | ~80 行 |
| **总计** | | ~175 行 |

### CSS 新增样式类
1. `.task-header-right` - 刷新按钮容器
2. `.refresh-btn` - 刷新按钮样式
3. `.refresh-loading` - 加载状态（旋转）
4. `@keyframes spin` - 旋转动画
5. `.task-overdue-tag` - 主任务超时标签
6. `.med-empty-state` - 空状态容器
7. `.empty-text` / `.empty-hint` / `.empty-btn` - 空状态文字/按钮
8. `.med-overdue` - 超时药品高亮
9. `.overdue-label` - 超时文字标签
10. `.overview-clickable` - 可点击概览项

---

## 验证状态

### ✅ 代码层面验证完成
- [x] AuthDialog.vue 回车键绑定（7 处）
- [x] BaseLayout.vue 关怀模式调整
- [x] Home.vue 核心函数实现（`refreshData`, `getOverdueMinutes`）
- [x] Home.vue computed 实现（`nextPendingOverdueMin`, `timeline` 过滤）
- [x] Home.vue 模板实现（刷新按钮、超时标签、空状态、点击跳转）
- [x] Home.vue CSS 样式（8 个新样式类 + 1 个动画）

### ⏳ 浏览器功能测试待执行
- [ ] 登录/注册回车键测试
- [ ] 关怀模式切换成员测试
- [ ] 手动刷新按钮测试
- [ ] 打卡后自动刷新测试
- [ ] 超时检测显示测试（需创建测试数据）
- [ ] 空状态引导卡片测试
- [ ] 概览卡点击跳转测试
- [ ] 关怀模式时间线过滤测试

### 💡 推荐测试顺序
1. **基础交互**（5 分钟）
   - 登录回车键
   - 注册回车键
   - 刷新按钮
   - 概览卡点击跳转

2. **关怀模式**（3 分钟）
   - 切换到 role=2 成员
   - 验证切换成员可见性
   - 验证时间线过滤

3. **空状态**（2 分钟）
   - 删除所有计划或新用户测试
   - 验证引导卡片显示
   - 验证按钮跳转

4. **超时检测**（5 分钟）
   - 创建过去时间的用药计划
   - 验证主任务卡超时标签
   - 验证用药列表超时高亮

---

## 浏览器测试指南

### 前置条件
```bash
# 1. 确认前端服务运行中
cd /Users/haifeng/coding/IdeaProjects/fcg/fcg-client
npm run dev

# 2. 访问地址
http://localhost:5173
```

### 测试数据
```
家庭账号: zhangfamily
密码: 123456

测试成员:
- 张爸爸 (id=1, role=0) - 超级管理员
- 张爷爷 (id=3, role=2) - 受控成员（关怀模式）
- 张奶奶 (id=11, role=2) - 受控成员（关怀模式）
```

### 完整测试流程

#### 第一部分：登录体验（5 分钟）

```
1. 打开 http://localhost:5173
2. 点击"登录/注册"按钮
3. 输入账号：zhangfamily
4. 按 Tab 键切换到密码框
5. 输入密码：123456
6. 按回车键
   ✓ 验证是否成功登录（不需要点击按钮）

7. 点击右上角"退出登录"
8. 点击"登录/注册"按钮
9. 切换到"注册"标签
10. 随意填写注册信息
11. 在任意输入框按回车键
    ✓ 验证是否触发注册（看到错误提示或成功提示）
```

#### 第二部分：首页功能（10 分钟）

```
1. 登录 zhangfamily
2. 选择"张爸爸"成员

3. 【刷新按钮】
   - 点击主任务卡右上角刷新按钮
   ✓ 验证：图标旋转、显示 Toast "刷新成功"

4. 【概览卡跳转】
   - 将鼠标悬停在"体征已录"区域
   ✓ 验证：鼠标变手型、卡片上移、背景变深
   - 点击该区域
   ✓ 验证：跳转到健康页

5. 【空状态】（如果有用药计划，先跳过此步）
   - 进入药品管理页
   - 删除所有用药计划
   - 返回首页
   ✓ 验证：显示空状态卡片
   ✓ 验证：显示"今日暂无用药计划" + 提示语
   ✓ 验证：显示"添加用药计划"按钮
   - 点击按钮
   ✓ 验证：跳转到药品页

6. 【打卡自动刷新】
   - 添加一个用药计划（如已删除）
   - 返回首页
   - 点击"立即打卡"
   ✓ 验证：显示 Toast "打卡成功 ✓"
   ✓ 验证：主任务卡自动更新为下一条待办

7. 【超时检测】（需创建测试数据）
   - 进入药品管理页
   - 创建一个用药计划，计划时间设为过去时间
     （例如：当前 10:00，设置计划时间 08:00）
   - 返回首页
   ✓ 验证：主任务卡显示"已超时 120 分钟"（红色标签）
   ✓ 验证：用药列表该药品：橙红色边框
   ✓ 验证：用药列表该药品：橙红色圆点
   ✓ 验证：用药列表该药品：显示"· 超时 120 分钟"（红色文字）
```

#### 第三部分：关怀模式（5 分钟）

```
1. 点击右上角用户头像
2. 点击"切换成员"
3. 选择"张爷爷"（role=2）

4. 【关怀模式激活】
   ✓ 验证：UI 字体变大（关怀模式自动激活）
   ✓ 验证：页面布局简化

5. 【切换成员可见性】
   - 点击右上角用户头像下拉菜单
   ✓ 验证："切换成员"选项仍然可见
   - 点击"切换成员"
   ✓ 验证：能够正常跳转到成员选择页

6. 【关怀模式时间线】
   - 返回首页
   - 跳过一个用药计划（点击"跳过"按钮）
   - 观察页面底部时间线（关怀模式独立显示）
   ✓ 验证：时间线不显示已跳过的项
   ✓ 验证：只显示待服和已服药品

7. 【对比标准模式】
   - 切换回"张爸爸"（role=0）
   - 观察时间线
   ✓ 验证：时间线显示所有项（包括已跳过）
```

---

## 已知限制与后续优化

### 当前实现范围
- ✅ 核心功能已全部实现到"可用状态"
- ✅ 代码已提交到 `dev` 分支并推送到远程
- ⏳ 浏览器功能测试待执行

### 后续优化方向

#### 1. 打卡动效优化（视觉增强）
- 打卡成功淡出动画（fadeOut transition）
- 勾选动画或粒子效果
- 提升打卡操作的愉悦感

#### 2. Loading 状态优化（用户体验）
- 使用 Element Plus Skeleton 骨架屏
- 替换当前的 "加载中…" 文字
- 提升首次加载体验

#### 3. 超时提醒增强（安全性）
- 超时 30 分钟以上显示"严重超时"红色高亮
- 超时 1 小时以上触发浏览器通知（Notification API）
- 增强对严重超时的警示

#### 4. 其他页面关怀适配（一致性）
- MedicineHome.vue - 药品页关怀模式大字体
- HealthHome.vue - 健康页关怀模式大字体
- 参考 Home.vue 的关怀模式样式实现

---

## 提交信息

```
commit 1f29074
Author: 管海峰
Date: 2026-03-18

feat(client): 首页优化与登录体验改进

- 登录/注册表单支持回车键快捷提交
- 关怀模式保留「切换成员」选项，防止老人被锁定
- 首页添加手动刷新按钮，打卡后自动刷新数据
- 超时未服药品高亮显示（橙红色边框+文字）
- 主任务卡显示「已超时 X 分钟」提示
- 空状态优化：显示引导卡片和「添加用药计划」按钮
- 概览卡体征录入区域支持点击跳转健康页
- 关怀模式时间线过滤掉已跳过项，只显示待服和已服

测试数据：zhangfamily/123456，测试成员：张爷爷（role=2，关怀模式）
```

**远程仓库**: `origin/dev`  
**推送状态**: ✅ 已推送

---

## 结论

**所有 7 项优化功能已完成实现并推送到远程仓库。**

### 代码实现：✅ 100% 完成
- 核心函数、computed、模板、CSS 样式全部实现
- 符合用户需求："可用状态"已达成

### 浏览器测试：⏳ 待执行
- 需要用户在本地浏览器执行完整测试流程
- 参考本文档第 9 节"浏览器测试指南"

### 下一步行动
1. **立即测试**（推荐）：按照测试指南执行浏览器功能测试
2. **继续开发**（可选）：实现后续优化功能（打卡动效、Loading 状态等）
3. **合并到 main**（发布）：充分测试后，合并到 main 分支触发 CI/CD 部署

---

**文档生成时间**: 2026-03-18  
**前端服务**: `http://localhost:5173` (已确认运行中)  
**数据库**: `47.121.181.198:3306/fcg_db` (已配置)
